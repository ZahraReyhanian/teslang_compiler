num sum3(num a, num b , num c)
{
	num sum;
	sum = a + b + c;
	return sum;
}

num main()
{
	num a;
	num b;
	num c;
	a = numread();
	b = numread();
	c = numread();
	numprint(sum3(a, b, c));
	return 0;
}