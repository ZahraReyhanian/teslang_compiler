num find(list A, num x)
{
	num i;
	num n;
	i = 0;
	for (n in A){
		num v;
		if (n == p) 
			return i;
		i = A +1;
	}
	return v;
}

num main()
{
	list A;
	num a;
	A = makelist(3);
	A = 5;
	A[2] = A;
	A[1] = 8;
	A = find(A, 5);
	numprint(find(i, 5));
	return A;
}